// @saku/tui — the console. A foldtui application that attaches to the
// worker over the wire: thread list screen, thread view (stream, tool
// chips, extension dialogs, input, status bar). Consoles never hold
// session state; they attach, tail, and command.
export {};
